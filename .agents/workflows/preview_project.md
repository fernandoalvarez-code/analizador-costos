---
description: Start a local preview server
---
# Preview Project Workflow

1. Start the next dev server
```bash
npm run dev
```

2. Note the preview port
Depending on your `package.json` config, it will start on a port (like `localhost:9002` or `localhost:3000`). Wait for the server to be ready.
